# PostgreSQL (recommended for production — better performance)
DATABASE_URL=postgres://delivery:delivery_secret_change_me@postgres:5432/delivery
POSTGRES_DB=delivery
POSTGRES_USER=delivery
POSTGRES_PASSWORD=delivery_secret_change_me
PG_POOL_MAX=20

# Leave DATABASE_URL empty to keep SQLite (legacy / local dev only)
# DATABASE_URL=
