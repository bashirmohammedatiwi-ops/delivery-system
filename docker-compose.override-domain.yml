# تشغيل على الدومين مع Let's Encrypt (https://demaalhayaadelivery.online)
# شغّل: bash scripts/init-ssl.sh أولاً، ثم:
# docker-compose -f docker-compose.yml -f docker-compose.override-domain.yml --profile https up -d

services:
  nginx:
    ports:
      - "80:80"
      - "443:443"
